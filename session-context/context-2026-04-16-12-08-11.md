# Session Context — 2026-04-16 12:08:11

(c) 2026 Sudhindra Anegondhi a.d.sudhindra@gmail.com

## Session Summary

Single very long session that closed **all of Phase 2** in
`docs/ROADMAP-phase2.md` — 11 sprints + the loose-ends cleanup from
2026-04-15, plus ADR-0013 follow-ups. 18 ADRs Completed. Test suite
grew **39 → 86** (+47). 25 commits pushed. Created `docs/V2-BACKLOG.md`
at the user's request to catalogue deferred items for a dedicated
post-Phase-2 review.

## Session Metadata

- **Working directory:** `/Users/sudhindra/projects/aiSpirit/consent-sheild`
- **Repo:** `github.com/SAnegondhi/consentshield` (main)
- **Starting commit:** `28523d8`
- **Latest commit at pause:** `c83831e` — `feat(ADR-0018): pre-built connectors — Mailchimp + HubSpot direct API`
- **Admin app:** `https://consentshield-one.vercel.app`
- **Demo sites:** `https://consentshield-demo.vercel.app`
- **Worker:** `https://cdn.consentshield.in/v1/*`
- **Supabase project:** `xlqiakmkdjycfiioslgs`
- **Upstash Redis:** `upstash-kv-citrine-blanket`

---

## Timeline (condensed)

### Part 1 — Loose ends (commits `2404833` → `0404b67`)
Deleted the stale `anegondhi@gmail.com` auth user. Commented out
the no-op `grant usage on schema auth` in migration
`20260414000010`. Promoted password-reset + email-change OTP HTML
to the then-to-be-created ops runbook. STATUS + session-handoff
cleaned.

### Part 2 — Sprint 2 ADR-0010 distributed rate limiter (commits `599e169` → `23fc160`)
`@upstash/redis@1.37.0` exact-pinned. Pipeline SET-NX-EX + INCR +
PTTL — one REST round trip. Fallback to Map with one-time warning.
User provisioned Upstash via Vercel Marketplace
(`upstash-kv-citrine-blanket`), shared 5 KV env vars. Smoke test
confirmed distributed path (5 allowed / 2 denied / no fallback
warning). Suite 39 → 43.

### Part 3 — Sprint 3 ADR-0012 Sprint 1 tests (commits `1dfaafc`, `67c1f7d`)
`tests/workflows/sla-timer.test.ts` — 7 tests for the
`set_rights_request_sla` trigger (boundary dates + 20-date property
sweep; switched to epoch compare to handle Postgres `.000` ms
trimming). `tests/rls/url-path.test.ts` — 5 tests closing S-2 (Org
A can't UPDATE/SELECT Org B's rights_request regardless of
predicate shape). Suite 43 → 55.

### Part 4 — Sprint 4 ADR-0011 deletion retry (commit `649d5dd`)
`check-stuck-deletions` Edge Function: per-org HMAC-SHA256 key
derivation (Deno Web Crypto) + `decrypt_secret` RPC for connector
config, re-POSTs to customer webhook with `[1h, 6h, 24h]` backoff,
marks `status='failed'` + `deletion_retry_exhausted` audit after 3
retries, 30-day DPDP cutoff. Schema adds `next_retry_at` + partial
index. Cron at `45 * * * *`.

**Incidental discoveries** while wiring:
- `pg_net` was NEVER enabled on the hosted Supabase project. All
  HTTP cron jobs had been silently failing with `schema "net" does
  not exist` for weeks. Fixed via new migration
  `20260416000003_enable_pg_net.sql`.
- Edge Function gateway rejects the vault-stored `sb_secret_*` key
  with `UNAUTHORIZED_INVALID_JWT_FORMAT`. Deployed with
  `--no-verify-jwt` as a workaround (documented in
  `reference_supabase_platform_gotchas.md`).

### Part 5 — Cron ops cleanup (commit `05ec7fc`)
Three cron jobs pointed at Edge Functions that were never built.
Redeployed `send-sla-reminders` with `--no-verify-jwt`; unscheduled
the three orphans (`stuck-buffer-detection`, `security-scan`,
`retention-check`) via migration `20260416000004`. Two of the
three have since been rebuilt and re-registered (security-scan in
Sprint 8; stuck-buffer + retention-check remain V2-O1).

### Part 6 — Sprint 5 ADR-0012 Sprint 2 Worker Miniflare (commit `7e72e6f`)
`miniflare@4.20260415.0` + `esbuild@0.28.0` (devDeps, exact-pinned).
`tests/worker/harness.ts` bundles `worker/src/index.ts` once per
suite run, boots Miniflare with a seeded KV + `outboundService`
that mocks every `/rest/v1/...` Supabase call. 10 events tests
(every HMAC + origin branch) + 4 banner tests (incl. ADR-0008
`no "secret" substring` invariant). `tsconfig.json` excludes
`tests/worker` from Next type-check (Cloudflare `RequestInit` vs
DOM). Suite 55 → 69.

### Part 7 — ADR-0013 final cleanup (commit `8dee427`)
Stale "pending" status text removed. Created
`docs/ops/supabase-auth-templates.md` — paste-ready OTP HTML for
all four Supabase email templates + pre-launch checklist.
Idempotency regression test explicitly deferred with a pointer to
V2-BACKLOG.

### Part 8 — First context save + doc updates (`ScheduleWakeup` pause)
`session-context/context-2026-04-16-07-01-59.md` written. Memory
updates: pg_net + JWT-format workaround + cron-inspection pattern
added to `reference_supabase_platform_gotchas.md`. New
`project_status_2026-04-16.md` supersedes 2026-04-15 version.

### Part 9 — Sprint 6 ADR-0012 Sprint 3 buffer tests (commit `52199c5`)
`tests/buffer/delivery.test.ts` (6 tests) exercises
`sweep_delivered_buffers`, `detect_stuck_buffers`,
`mark_delivered_and_delete` against `audit_log`. Delta-based
stuck-count assertion handles pre-existing dev data cleanly.
`tests/buffer/lifecycle.test.ts` (6 tests) confirms the
authenticated role can't UPDATE/DELETE buffer tables (migration
011 REVOKE still holds). Fix for `processing_log` column name
(`activity_name`, not `event_type`). Suite 69 → 81. **ADR-0012
Completed.**

### Part 10 — Sprint 7 ADR-0014 external services (commits `3e1457e`, `9d7085d`)
Code change: `src/lib/rights/email.ts` — removed the
`|| 'onboarding@resend.dev'` fallback, now throws if
`RESEND_FROM` unset. User provisioned:
- Cloudflare Turnstile widget (real site + secret keys in
  `.secrets` with `NEXT_PUBLIC_TURNSTILE_SITE_KEY` after a
  naming-fix rename).
- Razorpay test-mode account + 4 plans + webhook secret (7 env
  vars in `.secrets`).

Pushed 14 commits to main; Vercel auto-deployed; 9 env vars
force-pushed to Vercel Production (Preview env scoping quirk
deferred to V2-X1). Live verified:
- `curl /rights/<org>` contains real site key `0x4AAAAAAC...`
  (not always-pass test key).
- `POST /api/public/rights-request` with fake token returns
  `Turnstile rejected: invalid-input-response` from real
  Cloudflare.
- `POST /api/webhooks/razorpay` without signature returns 403
  (not 500).

### Part 11 — Sprint 8 ADR-0015 security posture scanner (commit `b0106d5`)
`run-security-scans` Edge Function (Deno, ~200 LoC, zero deps).
Per-property fetch + header inspection (HSTS/CSP/XFO/Referrer-Policy
+ TLS reachability). Writes per-finding rows into
`security_scans`; emits `posture_finding` audit events for
non-info findings. Re-registered `security-scan-nightly` cron.
Dashboard `/dashboard/enforcement` gains Security Posture section
with severity-colour-coded badges. Live smoke: 6 properties → 18
findings → 12 violations (missing_csp/xfo/referrer_policy; HSTS
present on all — Vercel default).

### Part 12 — Sprint 9 ADR-0016 consent probes v1 (commit `8819732`)
`run-consent-probes` Edge Function — static HTML analysis, two-pass
detection (structured `<script src>` + full-body substring for
URLs in inline JS). Classifies against `tracker_signatures`; flags
violations when detected category isn't consented AND sig is not
`is_functional`. Hourly cron at `10 * * * *`. Dashboard gains
Consent Probes section.

**Acceptance test outcome:**
- `/violator?violate=1` (consent_state=all-rejected) → 2 violations
  (GA4 + Meta Pixel) — PASS
- `/blog` (consent_state=analytics-rejected) → 1 violation
  (documented v1 false positive: GA4 URL appears in conditional JS
  block; static analysis can't distinguish conditional from
  unconditional → headless-browser v2 is V2-P1).

### Part 13 — V2-BACKLOG.md (commits `d9372da`, `293659f`)
User explicitly asked for deferred items to live in a separate
document for a dedicated post-Phase-2 review. Created
`docs/V2-BACKLOG.md` with 9 initial entries (V2-T1, V2-X1, V2-X2,
V2-P1, V2-P2, V2-O1, V2-O2, V2-O3, V2-K1). Added rule to CLAUDE.md
ADR workflow: deferrals go into the backlog, not buried in ADR
prose; the backlog is reviewed only after the phase closes.

### Part 14 — Sprint 10 ADR-0017 audit export Phase 1 (commit `2204367`)
`jszip@3.10.1` exact-pinned (rule #14 justification: conformant
ZIP encoding is 200+ LoC of careful binary layout). New table
`audit_export_manifests` (pointer-only — never stores ZIP bytes).
Security-definer `rpc_audit_export_manifest(p_org_id)` returns a
single JSONB with 8 sections. `POST /api/orgs/[orgId]/audit-export`
builds the ZIP in memory and returns it as an `attachment`. New
`/dashboard/exports` page. R2 upload path → V2-X3 (needs the
buffer-delivery pipeline which is Phase 3).

### Part 15 — Sprint 11 ADR-0018 pre-built connectors (commit `c83831e`)
`src/lib/rights/deletion-dispatch.ts` refactored into a per-type
switch. Three dispatchers:
- `dispatchWebhook` (existing logic moved, leaves status as
  `awaiting_callback`)
- `dispatchMailchimp` (DELETE with HTTP Basic auth +
  md5(email.toLowerCase()); 2xx or 404 → `confirmed`)
- `dispatchHubspot` (DELETE with Bearer token + idProperty=email;
  2xx or 404 → `confirmed`)

Integrations API route and dashboard form gained type selector +
per-type conditional fields. OAuth flow → V2-C1.
`tests/rights/connectors.test.ts` — 5 mocked-fetch tests. Fixed
one issue: `new Response('', {status: 204})` throws; use `null`
for 204/205/304. Suite 81 → 86.

---

## Commit History (this session, chronological)

```
2404833 chore: remove no-op grant usage on auth schema from migration 20260414000010
b38862b docs: log loose-end cleanup (migration 20260414000010 + stale auth user)
0404b67 docs: refresh STATUS.md to the 2026-04-16 pause point
599e169 feat(ADR-0010): sprint 1.1 — distributed rate limiter via Upstash Redis
b3b596f docs: log ADR-0010 Sprint 1.1 — index, CHANGELOG-api, STATUS
23fc160 feat(ADR-0010): sprint 1.1 completed — Upstash live, ADR closed
1dfaafc feat(ADR-0012): sprint 1 — SLA-timer + URL-path RLS test coverage
67c1f7d docs: log ADR-0012 Sprint 1 — index, CHANGELOG-schema, STATUS
649d5dd feat(ADR-0011): sprint 1 — deletion retry + timeout for stuck callbacks
05ec7fc chore(ops): fix cron jobs — redeploy sla-reminders, unschedule orphans
7e72e6f feat(ADR-0012): sprint 2 — Worker test harness with Miniflare
8dee427 docs(ADR-0013): close open items — ops runbook + stale status cleanup
52199c5 feat(ADR-0012): sprint 3 — buffer-pipeline + lifecycle REVOKE tests
3e1457e feat(ADR-0014): remove Resend onboarding@resend.dev fallback
9d7085d feat(ADR-0014): completed — Turnstile + Razorpay live on production
b0106d5 feat(ADR-0015): security posture scanner — headers + TLS nightly
8819732 feat(ADR-0016): consent probes v1 — static HTML analysis
d9372da docs: consolidate deferred-item backlog into docs/V2-BACKLOG.md
293659f docs(CLAUDE.md): record v2-backlog rule alongside ADR workflow
2204367 feat(ADR-0017): audit export package — Phase 1 direct-download ZIP
c83831e feat(ADR-0018): pre-built connectors — Mailchimp + HubSpot direct API
```

Plus 4 more from session-save commits that are intermixed.

---

## Migrations Applied (this session)

```
20260416000001 deletion_retry_state           (next_retry_at + partial index)
20260416000002 deletion_retry_cron            (hourly check-stuck-deletions)
20260416000003 enable_pg_net                  (create extension pg_net)
20260416000004 unschedule_orphan_crons        (drop 3 unbuilt-function jobs)
20260416000005 security_scan_cron             (re-schedule after function built)
20260416000006 consent_probes_cron            (hourly at :10)
20260416000007 audit_export                   (manifests table + aggregator RPC)
```

Total migrations on live DB: **27**, through
`20260416000007_audit_export.sql`.

---

## Dependencies Added (all exact-pinned)

- `@upstash/redis@1.37.0` — distributed rate limiter
- `miniflare@4.20260415.0` (dev) — Worker test harness
- `esbuild@0.28.0` (dev) — Worker bundling for Miniflare
- `jszip@3.10.1` — audit export ZIP assembly

All four individually justified under rule #14 in their ADRs.

---

## Edge Functions (4 live, all `--no-verify-jwt`)

- `send-sla-reminders` — existing, redeployed in Part 5
- `check-stuck-deletions` — new (ADR-0011)
- `run-security-scans` — new (ADR-0015)
- `run-consent-probes` — new (ADR-0016)

---

## Cron Jobs (6 live, all green)

Pure SQL:
- `buffer-sweep-15min` (every 15 min)
- `cleanup-unverified-rights-requests-daily`

HTTP via `net.http_post` (with vault-stored orchestrator key):
- `sla-reminders-daily` (02:30 UTC = 08:00 IST)
- `check-stuck-deletions-hourly` (`45 * * * *`)
- `security-scan-nightly` (20:30 UTC = 02:00 IST)
- `consent-probes-hourly` (`10 * * * *`)

---

## Test Suite (86 / 86)

| File | Tests | What |
|---|---|---|
| `tests/rls/isolation.test.ts` | 39 | Cross-tenant RLS |
| `tests/rls/url-path.test.ts` | 5 | S-2 URL-path RLS |
| `tests/rights/rate-limit.test.ts` | 4 | In-memory fallback |
| `tests/workflows/sla-timer.test.ts` | 7 | DB trigger + boundaries |
| `tests/worker/events.test.ts` | 10 | Worker events (Miniflare) |
| `tests/worker/banner.test.ts` | 4 | Banner compiled-output |
| `tests/buffer/delivery.test.ts` | 6 | Sweep + detect + mark |
| `tests/buffer/lifecycle.test.ts` | 6 | REVOKE enforcement |
| `tests/rights/connectors.test.ts` | 5 | Mailchimp + HubSpot |

---

## External Services (all live)

- **Resend** — DNS verified, relaxed-alignment DMARC, Gmail
  delivery confirmed. `RESEND_FROM=noreply@consentshield.in`.
- **Cloudflare Turnstile** — real site + secret keys on Vercel
  Production. Rejects fake tokens with `invalid-input-response`.
- **Razorpay** — test-mode account, 4 plans, webhook secret live.
  End-to-end checkout UX smoke → V2-X2.
- **Upstash Redis** — provisioned via Vercel Marketplace
  (`upstash-kv-citrine-blanket`), rate limiter confirmed using
  distributed path.

---

## V2 Backlog (11 entries, see `docs/V2-BACKLOG.md`)

- V2-T1 Signup idempotency regression test
- V2-X1 Vercel Preview env vars
- V2-X2 Razorpay end-to-end checkout smoke
- V2-X3 Audit-export R2 upload pipeline
- V2-P1 Headless-browser probe runner
- V2-P2 Probe CRUD UI
- V2-O1 Unbuilt cron-slot Edge Functions
- V2-O2 Vercel Deployment Protection
- V2-O3 pg_cron failure-detection watchdog
- V2-K1 Edge Function `--no-verify-jwt` workaround
- V2-C1 OAuth flow for pre-built connectors

**Do NOT pull from V2-BACKLOG mid-phase.** Rule codified in
CLAUDE.md and in `feedback_v2_backlog_pattern.md` memory.

---

## Memory Updates This Session

- Updated `MEMORY.md` index (project status pointer; added V2-backlog-pattern entry)
- Superseded `project_status_2026-04-16.md` (earlier version had Sprint 2 + 5 state; now reflects Phase-2 complete state)
- Created `feedback_v2_backlog_pattern.md` — captures the user's
  explicit request from 2026-04-16 to isolate v2 deferrals into a
  single doc and review 2–3 post-phase.

---

## Where to Pick Up Next

**Phase 2 is complete.** User's stated plan is a
**post-Phase-2 review**:

1. Walk the existing code end-to-end.
2. Pick **2–3 architecture decision points** from
   `docs/V2-BACKLOG.md` to graduate into the next phase's ADRs.
3. Close or down-grade the rest.

Out-of-phase Phase 3+ headline items (in roadmap "Out of scope"
section):
- Continuous buffer-delivery-to-R2 pipeline
- GDPR dual-framework
- ABDM module (healthcare; never persists FHIR per rule #3)

Recommended first action on next session: open the repo +
STATUS.md + V2-BACKLOG.md, walk the latest commits, then the user
picks which 2–3 V2 items become Phase-3 ADRs.

---

## Outstanding Known Issues

1. Vercel Deployment Protection off (V2-O2).
2. 2 orphan cron slots still need Edge Functions (V2-O1).
3. End-to-end Razorpay checkout UX smoke requires a non-dev test
   account (V2-X2).
4. Preview env vars on Vercel not scoped (V2-X1).
5. Consent probes can't distinguish conditional from unconditional
   tracker loads (V2-P1).

All are in V2-BACKLOG. None are blocking dev.
