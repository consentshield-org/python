# Session Context — 2026-04-15 22:02:12

(c) 2026 Sudhindra Anegondhi a.d.sudhindra@gmail.com

## Session Summary

Long-running session that closed nine blocking findings + nine should-fix
items from the 2026-04-14 codebase review (ADR-0008, ADR-0009), applied
all migrations to the live Supabase project, deployed two Vercel projects
(admin app at `consentshield-one.vercel.app`, demo customer sites at
`consentshield-demo.vercel.app`), and completed ADR-0013 Sprint 1 which
moves signup + login to OTP-only. Phase-2 roadmap written into
`docs/ROADMAP-phase2.md` with 11 numbered sprints. Session parked after
first live signup + dashboard-create-property verified working.

## Session Metadata

- **Working directory:** `/Users/sudhindra/projects/aiSpirit/consent-sheild`
- **Repo:** `github.com/SAnegondhi/consentshield` (main)
- **Latest commit at pause:** `28523d8` — `fix: public.current_uid() helper replaces auth.uid() in scoped-role RPCs`
- **Admin app:** `https://consentshield-one.vercel.app`
- **Demo sites:** `https://consentshield-demo.vercel.app`
- **Worker:** `https://cdn.consentshield.in/v1/*` (version 9fb7bd37)
- **Supabase project:** `xlqiakmkdjycfiioslgs`

---

## Timeline

### Part 1 — Review + blockers close (commits `788c63c` → `adcc184`)

1. Confirmed state: seven ADRs from 2026-04-14 all Completed. Commit 4bacdfa.
2. Ran a 5-agent parallel code review → `docs/reviews/2026-04-14-codebase-architecture-review.md`:
   - 9 Blocking (B-1..B-9), 13 Should-fix (S-1..S-13), areas passing clean.
3. Closed B-1/B-2/B-3 in `feat(ADR-0008): phase 1 — browser auth hardening`:
   - Removed `event_signing_secret` from compiled banner.js.
   - Added `origin_verified` column to `consent_events` + `tracker_observations`;
     Worker persists it.
   - Turnstile fails fast when `TURNSTILE_SECRET_KEY` unset in production;
     dev-only fallback logs a warning. 8-second fetch timeout added.
   - Migration 20260414000003 (column), 20260414000004 (rotate all
     event_signing_secret values).
4. Closed B-6 + partial B-4 via ADR-0009 Sprint 1.1 (`feat(ADR-0009): sprint 1.1`):
   - 3 security-definer RPCs owned by `cs_orchestrator`, granted to anon:
     `rpc_rights_request_create`, `rpc_rights_request_verify_otp`,
     `rpc_deletion_receipt_confirm`. The deletion RPC enforces the
     `awaiting_callback → confirmed` state machine (closes B-6).
   - Added missing cs_orchestrator grants on rights_requests.
5. Closed B-5/B-7/B-8/B-9 (`fix: close B-5, B-7, B-8, B-9`):
   - Razorpay webhook returns 422 on unresolvable org.
   - Missing buffer-sweep indexes on 6 tables.
   - Encryption RPC grants shifted from service_role to cs_orchestrator/cs_delivery.
   - pg_cron daily cleanup of unverified rights_requests.
6. Completed B-4 fully (`feat(ADR-0009): complete B-4`):
   - 7 more RPCs: rights portal read, privacy notice read,
     rights_event_append, banner_publish, integration_connector_create,
     signup_bootstrap_org, razorpay_apply_subscription, plan_limit_check.
   - Libraries (`gate.ts`, `crypto.ts`, `deletion-dispatch.ts`) take
     SupabaseClient parameter — no internal service-role client.
   - `grep -r SUPABASE_SERVICE_ROLE_KEY src/` returns zero matches.
7. Should-fix batch (`fix: should-fix batch`):
   - S-3 webhook dedup (`webhook_events_processed` + RPC).
   - S-6 60 s cache on derived keys in `crypto.ts`.
   - S-7 SLA Edge Fn requires `CS_ORCHESTRATOR_ROLE_KEY`.
   - S-10 ADR-0004/0005/0006 test-result sections written.
   - S-12 pg_cron secret via Supabase Vault (not `alter database set`).
8. Deployment fixups (`chore: deployment fixups`):
   - Supabase CLI `db push` FATAL'd on large RPC migration via Supavisor;
     fell back to direct `psql` over same pooler URL.
   - Two corrective migrations added:
     - `20260414000000_scoped_roles_set_option.sql` — PG 16 split GRANT
       ROLE; needed `with set true` + `create on schema public` grants.
     - `20260414000009_cron_vault_secret.sql` — `alter database set` is
       forbidden on hosted Supabase; switched cron jobs to read Vault.
   - `SUPABASE_ORCHESTRATOR_ROLE_KEY` → `CS_ORCHESTRATOR_ROLE_KEY`
     (Supabase reserves `SUPABASE_` prefix).
   - Worker redeployed to `cdn.consentshield.in`; smoke test confirmed
     banner.js no longer ships the signing secret and events land with
     `origin_verified='origin-only'`.
9. Deferred-items analysis (`docs: deferred-items analysis`):
   - S-1 → ADR-0010 (Vercel KV rate limiter)
   - S-2 → folded into S-11 as URL-path RLS test
   - S-5 → ADR-0011 (deletion retry Edge Fn)
   - S-11 → ADR-0012 (automated test suites)

### Part 2 — Refreshed STATUS + Phase-2 roadmap (commits `f850568`, `ebfc5f8`)

- `docs/STATUS.md` rewritten for 2026-04-15 pause point.
- `docs/ROADMAP-phase2.md` — 11 sprints, ADR map, out-of-scope noted.

### Part 3 — Vercel admin app live (commits `c3c0f67` → `266d885`)

1. Vercel CLI authentication done manually by user; `bunx vercel@latest
   link --project consentshield --yes` linked successfully after login.
2. 11 env vars pushed via `vercel env add <NAME> production preview --force`
   reading from `.secrets`. `SUPABASE_SERVICE_ROLE_KEY` deliberately
   excluded. Typo in `.secrets` noticed: `CLOURDFLARE_API_TOKEN` →
   `CLOUDFLARE_API_TOKEN` on Vercel side.
3. First `vercel deploy --prod` failed:
   `Git author <GitHub noreply email> must have access to the team`.
   Fixed by `git config user.email a.d.sudhindra@gmail.com` + new commit
   (`docs: refresh .env.local.example for Vercel + ADR-0008/0009 reality`).
4. Next deploy succeeded; 401 Vercel Authentication by default — user
   disabled Deployment Protection on the project settings.
5. `/privacy/<org>` + `/rights/<org>` returned 404 → debugged to
   `permission denied for schema auth`. Fix via migration
   `20260414000010_scoped_roles_rls_and_auth.sql`:
   - `grant usage on schema auth to cs_orchestrator, cs_delivery;`
     (emitted a warning but was essentially a no-op — the scoped roles
     still lacked USAGE. At the time we didn't realise the warning was
     the diagnostic; moved on with BYPASSRLS which did help.)
   - `alter role cs_orchestrator bypassrls;` + `alter role cs_delivery
     bypassrls;` — this is what actually unblocked the anon-granted RPCs
     that SELECT from RLS-protected tables.
6. `feat: root page is now a real landing` replaces the stub `/` with
   signup/login CTAs + a demo-sites link; redirects signed-in users.

### Part 4 — Demo customer sites (commit `fcb0de4`)

- New `test-sites/` directory with 5 scenarios + landing:
  - `/ecommerce` (GA4 + Hotjar + Meta Pixel + Razorpay), bottom-bar,
    full purposes.
  - `/saas` (Mixpanel + Segment + Intercom), modal, analytics +
    personalisation only.
  - `/blog` (GA4 only), bottom-right corner, analytics only.
  - `/healthtech` (no trackers), bottom-bar, essential only, monitoring
    off.
  - `/violator` (GA4 + Meta Pixel before consent when `?violate=1`).
- Plain static HTML, no build, one `vercel.json` with `cleanUrls: true`.
- New Vercel project `consentshield-demo` rooted at `test-sites/`;
  deployed at `consentshield-demo.vercel.app`. All six routes 200.
- DB: `ConsentShield Demo Customer` org
  (`432bca6d-8fce-415a-85e0-96397ddac666`) with 5 web_properties + 5
  consent_banners seeded, all `allowed_origins =
  ['https://consentshield-demo.vercel.app']`.

### Part 5 — Signup unblocker: ADR-0013 (commits `c3c0f67`, `6f9ea01`, `0ee7a80`, `28523d8`)

1. **Sprint 1.1** — single `/auth/callback` route for signup bootstrap.
   `/api/auth/signup` removed. Magic-link pattern.
2. **Sprint 1.2** — OTP-only signup + login. User chose OTP over magic
   link after security trade-off discussion. Signup stashes `org_name`
   + `industry` in `user_metadata`; `/auth/callback` reads it and runs
   `rpc_signup_bootstrap_org` idempotently.
3. Email deliverability debugging (long):
   - First signups all bounced at Gmail.
   - DMARC was `p=reject; adkim=s; aspf=s` (strict). Resend signs from
     `send.consentshield.in`; strict alignment vs root failed. Fixed:
     `adkim=r; aspf=r`.
   - DNS in Cloudflare export was correct, but live DKIM query returned
     empty `v=DKIM1; p=` — stale cache. Forced refresh resolved.
   - Direct Resend send from `consentshield.in` delivered to
     `anegondhi@gmail.com` (Resend account owner email). Probe worked.
   - Supabase Auth SMTP configured against Resend. User edited the
     "Magic Link" email template; had to also edit "Confirm signup"
     template (used for first-time signup, different from login).
   - Token is 8 digits (not 6 — Supabase project has
     `OTP_LENGTH=8` configured). Input `maxLength={6}` was truncating
     entries → "token expired or is invalid".
4. **OTP boxes UX** via `input-otp@1.4.2`. New
   `src/components/otp-boxes.tsx`; 8 boxes with iOS SMS autofill,
   paste, backspace-on-empty-focus-prev, arrow-key nav. Justification
   in commit message per rule #14.
5. **`public.current_uid()` helper** (migration `20260415000001`).
   Property-create route failed with `permission denied for schema auth`
   because `rpc_plan_limit_check` called `auth.uid()` as
   `cs_orchestrator`, and scoped roles don't have USAGE on `auth`
   (Supabase-owned schema). Helper reads the JWT claim via
   `current_setting('request.jwt.claims')` — same logic `auth.uid()`
   uses internally, no auth-schema dependency. Replaces `auth.uid()`
   in all five affected RPCs.
6. User verified end-to-end:
   - Signup with OTP boxes → code received → dashboard lands.
   - `/dashboard/properties` → create `https://consentshield-demo.vercel.app/ecommerce`
     → worked after the `current_uid` helper migration.

---

## Commit History (chronological, newest last)

```
788c63c feat(ADR-0008): phase 1 — browser auth hardening (B-1, B-2, B-3)
b21b0dc feat(ADR-0009): sprint 1.1 — scoped-role RPCs for public buffer writes (B-4 partial, B-6)
d50b98b fix: close B-5, B-7, B-8, B-9 from 2026-04-14 review
da0d168 feat(ADR-0009): complete B-4 — zero service-role usage in app code
adcc184 fix: should-fix batch from 2026-04-14 review (S-3, S-6, S-7, S-10, S-12)
d619c29 chore: deployment fixups after hosted-Supabase + pooler constraints
ac8b2de docs: deferred-items analysis — schedule S-1/S-5/S-11 into ADR-0010/0011/0012
f850568 docs: STATUS.md refreshed for the 2026-04-15 pause point
ebfc5f8 docs: Phase 2 roadmap — 11 sprints sequenced by dependency + ROI
c3c0f67 feat(ADR-0013): sprint 1 — single /auth/callback for signup bootstrap
dc6b2c3 docs: refresh .env.local.example for Vercel + ADR-0008/0009 reality
266d885 fix: scoped roles need BYPASSRLS + auth schema usage for security-definer RPCs
fcb0de4 feat: test-sites — 5 static demo customer pages for ConsentShield
90cfd5d feat: root page is now a real landing with signup/login + demo-sites link
6f9ea01 feat(ADR-0013): sprint 1.2 — OTP-only signup + login (supersedes magic link)
0ee7a80 feat(ADR-0013): OTP boxes UX + support variable token lengths
28523d8 fix: public.current_uid() helper replaces auth.uid() in scoped-role RPCs
```

---

## Migrations Applied (live)

Through `20260415000001`:

```
20260414000000 scoped_roles_set_option     (PG 16 SET option + public CREATE)
20260414000003 origin_verified              (columns on consent_events / tracker_observations)
20260414000004 rotate_signing_secrets       (regenerate all event_signing_secret)
20260414000005 scoped_rpcs_public           (3 anon-granted RPCs)
20260414000006 buffer_indexes_and_cleanup   (6 indexes + cleanup fn + encryption grants)
20260414000007 scoped_rpcs_authenticated    (7 more RPCs; encrypt/decrypt to authenticated)
20260414000008 webhook_dedup_and_cron_secret (webhook_events_processed + GUC cron)
20260414000009 cron_vault_secret            (replace GUC with Vault)
20260414000010 scoped_roles_rls_and_auth    (BYPASSRLS + auth schema usage — usage silently no-op'd)
20260415000001 request_uid_helper           (public.current_uid + replace auth.uid in 5 RPCs)
```

---

## Live DB State

- 3 orgs: `Estara-ai` (two — dev admin + original), `ConsentShield Demo Customer`.
- 6 pg_cron jobs active: buffer-sweep, cleanup-unverified-rights,
  retention-check, security-scan, sla-reminders, stuck-buffer-detection.
- Vault secret `cs_orchestrator_key` created.
- Tracker_signatures: 34 services seeded.
- Demo org has 5 web_properties + 5 consent_banners + 0 events yet.

---

## Vercel State

- Project `consentshield` (admin app, Next.js 16 + Turbopack on Node 24).
  11 env vars set. Deployment protection off.
- Project `consentshield-demo` (static test sites, rooted at `test-sites/`).
  No env vars needed.
- Both deployed from same GitHub repo on `main`.

---

## New Memory Files Created This Session

- `project_status_2026-04-15.md` (supersedes `project_status_2026-04-14.md`)
- `reference_vercel_setup.md`
- `reference_email_deliverability.md`
- `reference_supabase_platform_gotchas.md`
- `feedback_otp_over_magic_link.md`
- `feedback_no_auth_uid_in_scoped_rpcs.md`

`MEMORY.md` index updated to link all new memories.

---

## Where to Pick Up

Roadmap (`docs/ROADMAP-phase2.md`) enumerates Sprints 1–11. Sprint 1
(ADR-0013 signup) is now Complete pending a brief manual smoke test
of the happy path on a fresh email — the structural validation passed
and the property-create path worked end-to-end.

**Next up: Sprint 2 — Distributed rate limiter on Vercel KV (ADR-0010).**
~1 sprint, ~3 h. Deliverables in the roadmap.

Alternatively: Sprint 3 — SLA-timer property tests (ADR-0012 Sprint 1)
is the smallest useful bite if you prefer a low-risk kick-off.

---

## Outstanding Known Issues

1. `anegondhi@gmail.com` auth user exists from the early bounce-loop
   attempts with `email_verified: false` and no org membership. Harmless
   but clutters `auth.users`; safe to delete via SQL if it bothers.
2. The `grant usage on schema auth` from migration 20260414000010 is a
   silent no-op on hosted Supabase. Kept in the migration for
   documentation; should be removed or commented in a cleanup pass.
3. Supabase "Password reset" and "Email change" email templates are
   still stock (include links). Customise with `{{ .Token }}` before
   enabling those flows.
4. Vercel Deployment Protection is off on both projects — fine for dev,
   revisit before any real traffic.
