# Session Context — 2026-04-16 07:01:59

(c) 2026 Sudhindra Anegondhi a.d.sudhindra@gmail.com

## Session Summary

Single long session closing the 2026-04-15 loose ends and then
rotating through Sprints 2–5 of `docs/ROADMAP-phase2.md`: distributed
rate limiter (ADR-0010), SLA-timer + URL-path RLS test coverage
(ADR-0012 Sprint 1), deletion retry Edge Function (ADR-0011), cron
ops cleanup (pg_net + JWT-format + orphan job removal), Worker
Miniflare tests (ADR-0012 Sprint 2), and ADR-0013 final cleanup
(ops runbook + deferred-test note). Test suite grew 39 → 69
across this session.

## Session Metadata

- **Working directory:** `/Users/sudhindra/projects/aiSpirit/consent-sheild`
- **Repo:** `github.com/SAnegondhi/consentshield` (main)
- **Starting commit:** `28523d8`
- **Latest commit at pause:** `8dee427` — `docs(ADR-0013): close open items — ops runbook + stale status cleanup`
- **Admin app:** `https://consentshield-one.vercel.app`
- **Demo sites:** `https://consentshield-demo.vercel.app`
- **Worker:** `https://cdn.consentshield.in/v1/*`
- **Supabase project:** `xlqiakmkdjycfiioslgs`
- **Upstash Redis:** `upstash-kv-citrine-blanket` (Vercel Marketplace)

---

## Timeline

### Part 1 — Loose ends from 2026-04-15 pause (commits `2404833`, `b38862b`, `0404b67`)

1. **Stale auth user deleted.** `anegondhi@gmail.com`
   (`cde31bea-734b-4796-ab3a-be490ac04b8b`, unconfirmed, 0
   memberships) removed via direct psql.
2. **Migration 20260414000010 cleaned up.** The `grant usage on
   schema auth to cs_orchestrator, cs_delivery` line was a silent
   no-op (auth schema is owned by `supabase_auth_admin`). Commented
   it out with an explanatory block. BYPASSRLS statements retained.
3. **Password reset + email change templates documented.** First
   pasted into `reference_email_deliverability.md` memory; later
   promoted to `docs/ops/supabase-auth-templates.md` (see Part 7).
4. Cleaned drift in `.claude/session-handoff.md`, `CHANGELOG-schema.md`,
   and `docs/STATUS.md` (the "Signup 401" bug entry had been resolved
   by ADR-0013 but was still listed).

### Part 2 — ADR-0010 distributed rate limiter (commits `599e169`, `b3b596f`, `23fc160`)

1. Drafted ADR with Upstash-via-Vercel-Marketplace decision (Vercel
   KV was retired in 2025).
2. Installed `@upstash/redis@1.37.0` exact-pinned.
3. Rewrote `src/lib/rights/rate-limit.ts`:
   - Pipeline of `SET NX EX` + `INCR` + `PTTL` — one REST round trip.
   - Falls back to the original in-memory `Map` when env vars unset;
     one-time console warning marks dev-only path.
   - Signature became `async`. Both public rights-request routes
     now `await checkRateLimit(...)`. Key prefix: `rl:`.
4. Added 4 Vitest cases covering the fallback path. `vi.stubEnv` +
   `vi.resetModules()` + dynamic import dance to force the module
   onto the fallback branch for deterministic testing.
5. User provisioned Upstash via Vercel Dashboard after the code
   landed; shared the five `KV_*` env vars in chat. Appended them
   to `.env.local` (gitignored). Smoke test
   `scripts/smoke-test-rate-limit.ts` showed 5 allowed / 2 denied
   with no fallback warning — distributed path confirmed. ADR
   marked Completed.

### Part 3 — ADR-0012 Sprint 1 tests (commits `1dfaafc`, `67c1f7d`)

1. Drafted ADR-0012 with three phases: SLA-timer + URL-path RLS
   (Sprint 1, this session), Worker Miniflare (Sprint 2), buffer
   pipeline (Sprint 3, still deferred).
2. `tests/workflows/sla-timer.test.ts` — 7 tests exercising the
   `set_rights_request_sla` Postgres trigger:
   - Six boundary dates (mid-year, year-crossing, leap/non-leap Feb,
     IST-anchored, month-boundary overflow).
   - 20-date property sweep over 2026–2030.
   - Early false-positive: Postgres drops `.000` milliseconds in
     `timestamptz` output; switched to `Date.getTime()` epoch
     comparison.
3. `tests/rls/url-path.test.ts` — 5 tests covering S-2: Org A
   authenticated client cannot SELECT or UPDATE Org B's
   `rights_request` with or without a fake `.eq('org_id', orgB)`
   predicate. Admin re-read confirms Org B row untouched.

Suite 43 → 55.

### Part 4 — ADR-0011 deletion retry (commits `649d5dd`, `05ec7fc`)

1. Drafted ADR-0011: hourly `check-stuck-deletions` Edge Function
   that re-POSTs to customer webhooks with `[1h, 6h, 24h]` backoff,
   marks `status='failed'` after 3 failures with
   `deletion_retry_exhausted` audit event, skips receipts older
   than 30 days (DPDP SLA).
2. Migration `20260416000001_deletion_retry_state.sql`: adds
   `next_retry_at` column, partial index on `(next_retry_at) where
   status = 'awaiting_callback'`, extends cs_orchestrator UPDATE
   grant to include the new column.
3. Migration `20260416000002_deletion_retry_cron.sql`: hourly
   cron at `45 * * * *`.
4. `supabase/functions/check-stuck-deletions/index.ts` — full Deno
   implementation with per-org HMAC-SHA256 key derivation (Web
   Crypto) + `decrypt_secret` RPC to read connector config. Same
   payload shape as the Next.js dispatcher.
5. Deploy surfaced two pre-existing dev-infra issues:
   - **`pg_net` was never enabled** on the hosted Supabase project.
     All HTTP cron jobs had been silently failing with `schema "net"
     does not exist` for weeks. Buffer-sweep worked because it's
     pure SQL. Fixed via new migration
     `20260416000003_enable_pg_net.sql`.
   - **Edge Function gateway rejects `sb_secret_*` keys** with
     `UNAUTHORIZED_INVALID_JWT_FORMAT`. The vault-stored
     `cs_orchestrator_key` is in the new format; the gateway still
     expects a legacy JWT. Deployed `check-stuck-deletions` with
     `--no-verify-jwt` as a workaround.
6. Live smoke via `net.http_post` from psql: 200 OK,
   `{"ok":true,"scanned":0,...}`. ADR-0011 marked Completed.

### Part 5 — Cron ops cleanup (commit `05ec7fc`)

1. Discovered during ADR-0011 verification: of the four HTTP cron
   jobs, only `send-sla-reminders` had a matching deployed function.
   Three others (`check-stuck-buffers`, `run-security-scans`,
   `check-retention-rules`) were never built.
2. Redeployed `send-sla-reminders` with `--no-verify-jwt`. Live
   smoke: 200 OK `{"sent":0}`.
3. Dropped three orphan cron entries via migration
   `20260416000004_unschedule_orphan_crons.sql`. Will be
   re-registered alongside ADR-0015 (security scanner) +
   Phase-3 retention features.
4. Cron inventory now 4 jobs, all green.

### Part 6 — ADR-0012 Sprint 2 Worker Miniflare (commit `7e72e6f`)

1. Installed `miniflare@4.20260415.0` + `esbuild@0.28.0`, both
   exact-pinned.
2. `tests/worker/harness.ts` — Miniflare factory that bundles
   `worker/src/index.ts` via esbuild once per suite run, boots it
   in Miniflare with a seeded KV namespace + `outboundService` that
   intercepts every `/rest/v1/...` fetch with an in-memory mock.
   Exposes `signHmac` for test signatures.
3. `tests/worker/events.test.ts` — 10 tests covering every
   HMAC + origin branch of `POST /v1/events`.
4. `tests/worker/banner.test.ts` — 4 tests for `GET /v1/banner.js`
   including the ADR-0008 invariant: compiled output has no
   `"secret"` substring or the signing secret value.
5. `tsconfig.json` excludes `tests/worker` — miniflare's
   Cloudflare-flavoured `RequestInit` conflicts with DOM's during
   `next build`. Vitest unaffected.

Suite 55 → 69.

### Part 7 — ADR-0013 final cleanup (commit `8dee427`)

1. Stale "pending live manual signup test" language removed from
   both the Status header and Sprint 1.2 status line.
2. Created `docs/ops/supabase-auth-templates.md` — paste-ready HTML
   for all four Supabase templates (Confirm signup / Magic Link /
   Reset Password / Change Email Address) with a pre-launch
   checklist. STATUS's Pending Manual Setup now points here.
3. Explicitly deferred the signup-idempotency regression test.
   Reason: the guard lives in `src/app/auth/callback/route.ts`, not
   the RPC. Proper test requires Next-route-handler infra which
   this repo doesn't have yet. Will ride ADR-0012 Sprint 3 or a
   dedicated follow-up ADR.

---

## Commit History (this session, chronological)

```
2404833 chore: remove no-op grant usage on auth schema from migration 20260414000010
b38862b docs: log loose-end cleanup (migration 20260414000010 + stale auth user)
0404b67 docs: refresh STATUS.md to the 2026-04-16 pause point
599e169 feat(ADR-0010): sprint 1.1 — distributed rate limiter via Upstash Redis
b3b596f docs: log ADR-0010 Sprint 1.1 — index, CHANGELOG-api, STATUS
23fc160 feat(ADR-0010): sprint 1.1 completed — Upstash live, ADR closed
1dfaafc feat(ADR-0012): sprint 1 — SLA-timer + URL-path RLS test coverage
67c1f7d docs: log ADR-0012 Sprint 1 — index, CHANGELOG-schema, STATUS
649d5dd feat(ADR-0011): sprint 1 — deletion retry + timeout for stuck callbacks
05ec7fc chore(ops): fix cron jobs — redeploy sla-reminders, unschedule orphans
7e72e6f feat(ADR-0012): sprint 2 — Worker test harness with Miniflare
8dee427 docs(ADR-0013): close open items — ops runbook + stale status cleanup
```

---

## Migrations Applied (this session)

```
20260416000001 deletion_retry_state          (next_retry_at + partial index)
20260416000002 deletion_retry_cron           (hourly check-stuck-deletions at 45 * * * *)
20260416000003 enable_pg_net                 (create extension pg_net)
20260416000004 unschedule_orphan_crons       (drop 3 unbuilt-function cron jobs)
```

Total migrations applied to the live DB: **25**, through
`20260416000004`.

---

## Edge Functions

Both deployed with `--no-verify-jwt`:
- `send-sla-reminders` (redeployed this session for the flag)
- `check-stuck-deletions` (new this session)

Four other functions referenced by dropped cron jobs were never
built and will appear with their feature ADRs.

---

## Test Suite

**69 / 69 passing** across 6 files:
- `tests/rls/isolation.test.ts` — 39
- `tests/rls/url-path.test.ts` — 5  (new this session)
- `tests/rights/rate-limit.test.ts` — 4  (new this session)
- `tests/workflows/sla-timer.test.ts` — 7  (new this session)
- `tests/worker/events.test.ts` — 10 (new this session, Miniflare)
- `tests/worker/banner.test.ts` — 4  (new this session, Miniflare)

---

## Memory Updates This Session

Updated:
- `reference_supabase_platform_gotchas.md` — added pg_net enable
  requirement, Edge-Function gateway JWT-format workaround,
  cron.job_run_details join pattern, migration-ordering note.

Created:
- `project_status_2026-04-16.md` (supersedes 2026-04-15 version,
  which has been removed along with stale 2026-04-14)

MEMORY.md index pointer refreshed.

---

## Where to Pick Up

Roadmap (`docs/ROADMAP-phase2.md`) enumerates Sprints 1–11.
Sprints 1, 2, 3, 4, 5 are now Complete; Sprint 6 (ADR-0012 Sprint 3
buffer-pipeline integration tests) is the natural continuation of
the test-coverage track. Sprint 7 is ADR-0014 external-service
activation (Turnstile prod, Razorpay, production domain); that
switches into feature / launch readiness work.

Recommended: **Sprint 6** if continuing test coverage — it also
unlocks the deferred signup-idempotency test from ADR-0013 by
giving us Next-route-handler test infra. **Sprint 7** if launch
readiness is the higher priority.

---

## Outstanding Known Issues

1. Vercel Deployment Protection still off on both projects (fine
   for dev, revisit before real traffic).
2. Turnstile production keys + Razorpay live keys still pending
   (ADR-0014).
3. Password reset / email change UI not yet surfaced; templates in
   `docs/ops/supabase-auth-templates.md` must be pasted into the
   Supabase Dashboard before those flows ship.
4. Signup-idempotency regression test deferred (see above).
5. Three Edge Function stubs (`check-stuck-buffers`,
   `run-security-scans`, `check-retention-rules`) referenced in
   cron jobs that were dropped — must be built alongside their
   respective ADRs and the cron entries re-registered then.
