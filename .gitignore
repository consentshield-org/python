# Dependencies
node_modules/
.pnp
.pnp.js

# Build
.next/
app/.next/
admin/.next/
out/
dist/
build/

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.e2e
.env.partner
app/.env.local
admin/.env.local
.secrets
.secrets*

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Vercel
.vercel

# Supabase
supabase/.temp/
# Supabase CLI auto-init cruft from shell-startup invocations while cwd is test-sites/
test-sites/supabase/

# Cloudflare
worker/.wrangler/
worker/.dev.vars
worker/.dev.vars.local

# Stryker mutation testing — generated reports + per-run temp dir
worker/reports/
worker/.stryker-tmp/
app/reports/
app/.stryker-tmp/
app/.stryker-tmp-delivery/
app/.stryker-tmp-v1/
.stryker-tmp/
reports/mutation/

# Sentry
.sentryclirc

# Test coverage
coverage/

# TypeScript
*.tsbuildinfo

# Archive
files.zip

# OpenWolf — token ledger grows unboundedly per session; keep local only
.wolf/token-ledger.json

# Unzipped scratch dirs
docs/design/consentshield-zip/

# Claude Code telemetry / local usage tracking
claude-usage/

# Transient session dumps from Claude Code / OpenWolf — disposable
session-context/

# MS Office lockfiles
~$*

# sed -i.bak leftovers
*.bak
