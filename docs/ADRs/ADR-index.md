# ADR Index

All Architecture Decision Records for ConsentShield, in chronological order.

| ADR | Title | Status | Date | Phases | Sprints |
|-----|-------|--------|------|--------|---------|
| ADR-0001 | Project Scaffolding — Next.js, Supabase Schema, Auth, Worker Skeleton | Completed | 2026-04-13 | 3 | 7 |
| ADR-0002 | Worker HMAC Verification + Origin Validation | Completed | 2026-04-13 | 1 | 3 |
| ADR-0003 | Consent Banner Builder + Compliance Dashboard | Completed | 2026-04-14 | 2 | 5 |
| ADR-0004 | Rights Request Workflow (Turnstile + OTP + Dashboard Inbox) | Completed | 2026-04-14 | 2 | 4 |
| ADR-0005 | Tracker Monitoring (Banner Script v2 with MutationObserver) | Completed | 2026-04-14 | 1 | 3 |
| ADR-0006 | Razorpay Billing + Plan Gating | Completed | 2026-04-14 | 1 | 3 |
| ADR-0007 | Deletion Orchestration (Generic Webhook Protocol) | Completed | 2026-04-14 | 1 | 3 |
| ADR-0008 | Browser Auth Hardening (Remove Client Signing Secret, origin_verified, Fail-Fast Turnstile) | Completed | 2026-04-14 | 1 | 4 |
| ADR-0009 | Scoped-Role Enforcement in REST Paths | Completed | 2026-04-14 | 3 | 3 |
| ADR-0010 | Distributed Rate Limiter for Public Rights-Request Endpoints | Completed | 2026-04-16 | 1 | 1 |
| ADR-0011 | Deletion Retry and Timeout for Stuck Callbacks (hourly Edge Function + 1h/6h/24h backoff) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0012 | Automated Test Suites for High-Risk Paths (SLA trigger, URL-path RLS, Worker, buffer pipeline) | Completed | 2026-04-16 | 1 | 3 |
| ADR-0013 | Signup Bootstrap Hardening (OTP signup + login, single `/auth/callback` path) | Completed | 2026-04-15 | 1 | 2 |
| ADR-0014 | External Service Activation (Resend / Turnstile / Razorpay) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0015 | Security Posture Scanner (run-security-scans Edge Function + dashboard) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0016 | Consent Probes v1 (static HTML analysis; run-consent-probes Edge Function + dashboard) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0017 | Audit Export Package (Phase 1: direct-download ZIP; R2 upload → V2-X3) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0018 | Pre-built Deletion Connectors (Mailchimp + HubSpot via API key; OAuth → V2-C1) | Completed | 2026-04-16 | 1 | 1 |
| ADR-0019 | DEPA Roadmap — Charter & Sequencing of ADR-0020..0025 (meta-ADR, no code) | Completed | 2026-04-18 | — | — | _(all child ADRs 0020/0021/0022/0023/0024/0025/0037 shipped)_ |
| ADR-0020 | DEPA Schema Skeleton (6 new tables + §11.3 ALTERs + helpers + non-dispatch triggers + shared types) | Completed | 2026-04-17 | 1 | 1 |
| ADR-0021 | `process-consent-event` Edge Function + Dispatch Trigger + Safety-Net Cron | Completed | 2026-04-17 | 1 | 1 |
| ADR-0022 | `process-artefact-revocation` Edge Function + Revocation Dispatch | Completed | 2026-04-17 | 1 | 4 |
| ADR-0023 | DEPA Expiry Pipeline (`send_expiry_alerts` + `enforce_artefact_expiry` + pg_cron) | Completed | 2026-04-17 | 1 | 2 |
| ADR-0024 | DEPA Customer UI Rollup (Purposes + Artefacts + Dashboard tile + Rights reshape + Settings row) | Completed | 2026-04-17 | 1 | 4 |
| ADR-0025 | DEPA Score Dimension — nightly refresh + API + dashboard gauge | Completed | 2026-04-17 | 1 | 2 |
| ADR-0026 | Monorepo Restructure (Bun workspace — `app/` + `admin/` + `packages/*`) | In Progress | 2026-04-16 | 4 | 4 |
| ADR-0027 | Admin Platform Schema (`cs_admin` role + `admin.*` tables + audit log + impersonation) | Completed | 2026-04-17 | 4 | 5 |
| ADR-0028 | Admin App Foundation (real OTP auth + Operations Dashboard + Audit Log viewer) | Completed | 2026-04-17 | 3 | 3 |
| ADR-0029 | Admin Organisations (list + detail + actions + impersonation + customer-side cross-refs) | Completed | 2026-04-17 | 4 | 4 |
| ADR-0030 | Sectoral Templates (admin panel + customer-side read) | Completed | 2026-04-17 | 3 | 3 |
| ADR-0032 | Support Tickets (admin panel + customer-side submit) | Completed | 2026-04-17 | 2 | 2 |
| ADR-0036 | Feature Flags & Kill Switches (admin panel) | Completed | 2026-04-17 | 1 | 1 |
| ADR-0037 | DEPA Completion (expiry fan-out + per-requestor binding + CSV + Audit DEPA + Onboarding seed pack) | Completed | 2026-04-17 | 1 | 5 |
| ADR-0038 | Operational Observability (cron failure watchdog + stuck-buffer Edge Function) | Completed | 2026-04-17 | 1 | 3 |
| ADR-0040 | Audit R2 Upload Pipeline (sigv4 + export_configurations UI + delivery-target branch) | Completed | 2026-04-17 | 1 | 4 |
| ADR-0042 | Signup Idempotency Regression Test (ensureOrgBootstrap helper + unit test) | Completed | 2026-04-17 | 1 | 1 |
| ADR-0041 | Probes v2 (Vercel Sandbox runner + probe CRUD UI) | Completed | 2026-04-17 | 1 | 5 |
| ADR-0039 | Connector OAuth (Mailchimp + HubSpot) | Completed | 2026-04-17 | 1 | 3 |
| ADR-0031 | Connector Catalogue + Tracker Signature Catalogue (admin panels) | Completed | 2026-04-17 | 2 | 4 |
| ADR-0033 | Admin Ops + Security — Pipeline Operations + Abuse & Security panels (folds ADR-0035) | Completed | 2026-04-17 | 2 | 5 |
| ADR-0034 | Admin Billing Operations — Razorpay failures + refunds + comps + plan overrides | Completed | 2026-04-18 | 2 | 3 |
| ADR-0035 | Abuse & Security admin panel | Abandoned | 2026-04-17 | — | — | _(folded into ADR-0033; see its context)_ |
| ADR-0043 | Customer App is Auth-Only (drop public landing; marketing site becomes www.consentshield.in) | Completed | 2026-04-17 | 1 | 1 |
| ADR-0044 | Customer RBAC — 4-level hierarchy (account → orgs → web properties) + 5-role model + invitation-only signup | Completed | 2026-04-18 | 3 | 7 |
| ADR-0045 | Admin user lifecycle (invite + role change + disable) | Completed | 2026-04-18 | 2 | 3 |
| ADR-0046 | Significant Data Fiduciary foundation — sdf_status on organisations + DPIA records + auditor engagements | Completed | 2026-04-20 | 4 | 5 |
| ADR-0047 | Customer membership lifecycle (role change + remove) + membership_audit_log + single-account-per-identity invariant | Completed | 2026-04-18 | 1 | 2 |
| ADR-0048 | Admin Accounts panel + ADR-0033/34 deviation closeout (suspend_account / account picker / Worker HMAC+Origin logging) | Completed | 2026-04-18 | 2 | 3 |
| ADR-0049 | Security observability ingestion — rate_limit_events + sentry_events (closes V2-S1/S2) | Completed | 2026-04-18 | 2 | 4 |
| ADR-0050 | Admin account-aware billing — issuer entities + invoices + GST + dispute workspace | Completed | 2026-04-20 | 3 | 6 |
| ADR-0051 | Billing evidence ledger — chargeback-defense capture points (triggers on audit_log / webhook_events / invoices / accounts / rights_requests / banners) | Completed | 2026-04-20 | 1 | 2 |
| ADR-0052 | Razorpay dispute contest submission — prepare packet + auto-submit via Razorpay Documents + Contest APIs | Completed | 2026-04-20 | 1 | 2 |
| ADR-0053 | GSTR-1 JSON export (monthly filing) — GSTN Offline-Utility v3.2 shape; b2b / b2cl / b2cs / hsn / doc_issue | Completed | 2026-04-20 | 1 | 1 |
| ADR-0055 | Account-scoped impersonation — target_account_id column + `start_impersonation_account` + target_scope in customer session list | Completed | 2026-04-20 | 1 | 1 |
| ADR-0056 | Per-account feature-flag targeting — new 'account' scope in admin.feature_flags; resolver fallback org → account → global | Completed | 2026-04-21 | 1 | 2 |
| ADR-0054 | Customer-facing billing portal (invoice history + billing profile) | Completed | 2026-04-20 | 1 | 2 |
| ADR-0057 | Customer-facing sectoral template switcher (Settings → Account) | Completed | 2026-04-20 | 1 | 1 |
| ADR-0058 | Split-flow customer onboarding — marketing-site intake + customer-app 7-step wizard reusing public.invitations | Completed | 2026-04-21 | 1 | 5 |
| ADR-1001 | v2 Whitepaper Phase 1 — Truth-in-marketing + Public API foundation (`cs_live_*` keys + Bearer middleware) | Completed | 2026-04-19 | 3 | 7 |
| ADR-1002 | v2 Whitepaper Phase 2 — DPDP §6 runtime enforcement (`/v1/consent/verify` + `record` + artefact ops + deletion API) | Completed | 2026-04-19 | 5 | 8 |
| ADR-1003 | v2 Whitepaper Phase 3 — Processor posture (`storage_mode` enforcement + BYOS + Zero-Storage + Healthcare seed + sandbox). **Phase 1 (Sprints 1.1-1.4) shipped 2026-04-24/25** (storage_mode resolver + KV cache + Worker branches + rpc_consent_record fence). **Phase 2 (Sprints 2.1-2.2) shipped 2026-04-24** (BYOK scope-down probe + BYOS customer docs + Standard→Insulated runbook). **Phase 3 Sprint 3.1 shipped 2026-04-24** (zero-storage hot-row TTL refresh). **Phase 4 Sprint 4.1 shipped 2026-04-25** (Healthcare Starter template + admin.sectoral_templates default_storage_mode gate + connector_defaults + customer onboarding doc + Phase 1 operator runbook). **Phase 4 Sprint 4.2 shipped 2026-04-26** (admin template editor extended with default_storage_mode radio + connector_defaults JSON editor; migrations 61 + 62 amend the draft RPCs; 6/6 integration tests). **Phase 5 Sprint 5.1 shipped 2026-04-25** in three rounds (organisations.sandbox column + provisioning RPC + cs_test_* key issuance + dashboard page + test-principal endpoint at `/api/v1/sandbox/test-principals` + audit-export manifest.sandbox marker + depa_compliance_metrics_prod cross-customer view + customer doc + sandbox-mode banner). **Phase 3 Sprint 3.2 calibration round shipped 2026-04-26** (k6 + bun harness at `tests/load/`, gap inventory `docs/design/zero-storage-feature-matrix.md`, benchmark `docs/benchmarks/zero-storage-100k.md`, first live calibration: Mode B 500/500 PASS @ p95=9.6s / buffer-row max 6 of 20; Mode A blocked on recurring CF Hyperdrive half-open-pool bug — operator action). Sprint 3.2 partial: full 100K Mode B (~7h overnight) + Mode A (post-Hyperdrive recreation) + 4-week observation pending. ADR-1003 v1-launch ready. | In Progress | 2026-04-19 | 5 | 10 |
| ADR-1004 | v2 Whitepaper Phase 4 — Statutory retention (Regulatory Exemption Engine) + material-change re-consent + silent-failure detection; Phase 1 Sprints 1.1-1.5 + Phase 3 Sprints 3.1-3.2 shipped 2026-04-22; Phase 2 Sprint 2.1 shipped 2026-04-22 (notices schema + publish_notice auto-versioning); **Phase 2 Sprints 2.2 + 2.3 shipped 2026-04-23** (wireframes at `consentshield-notices.html`; `/dashboard/notices` list + publish form + CSV export; replaced_by pipeline via `mark_replaced_artefacts_for_event` wired into `process-consent-event`; `reconsent_campaigns` table + nightly refresh cron + `/dashboard/notices/[id]/campaign` view; 7/7 integration tests PASS). Only Sprint 1.6 (legal counsel review of regulatory_exemptions defaults) remains. | In Progress | 2026-04-19 | 3 | 9 |
| ADR-1005 | v2 Whitepaper Phase 5 — Operations maturity. Phase 2 Sprint 2.1 (`/v1/integrations/{connector_id}/test_delete`), Phase 5 Sprint 5.1 (`/v1/rights/requests`), Phase 6 Sprint 6.1 (NotificationAdapter scaffold) all shipped 2026-04-22. **Phase 6 Sprints 6.2 + 6.3 + 6.4 shipped 2026-04-23**: five real adapters (Slack/Teams/Discord/PagerDuty/custom_webhook) + live Slack verification + 51 unit tests + `/dashboard/settings/notifications` CRUD + per-channel test-send + per-alert-type subscription matrix. Phase 4 status-page subsumed by ADR-1018. Phase 1 (partner case study) + Phase 3 (SLA / SE / PagerDuty for own-ops) remain external blockers tracked on admin.ops_readiness_flags. | In Progress | 2026-04-19 | 6 | 10 |
| ADR-1006 | v2 Whitepaper Phase 6 — Developer experience: client libraries (Node + Python + Go; **scope amendment 2026-04-25** drops Java in favour of OpenAPI-driven future generation) + OpenAPI spec (already at `marketing/public/openapi.yaml`) + CI drift check. **Phase 1 Sprint 1.1 complete 2026-04-25** — `packages/node-client/` Bun workspace scaffolded (`@consentshield/node` v1.0.0-alpha.1). `ConsentShieldClient` constructor with strict validation (`cs_live_` prefix check, positive `timeoutMs`, non-negative integer `maxRetries`) + `CONSENT_VERIFY_FAIL_OPEN` env override + readonly resolved-config exposure. `HttpClient` transport: 2 s default timeout (compliance posture per v2 whitepaper §5.4), exponential backoff (100 / 400 / 1 600 ms) on 5xx + transport errors, **never** retries 4xx or timeouts, caller `AbortSignal` honoured + composed, RFC 7807 problem-document parsing onto `error.problem`, `X-CS-Trace-Id` request + response round-trip (ADR-1014 Sprint 3.2). Five-class error hierarchy: `ConsentShieldError` base + `ConsentShieldApiError` + `ConsentShieldNetworkError` + `ConsentShieldTimeoutError` + `ConsentVerifyError` (compliance-critical, wraps cause when verify fails closed). `ping()` against `/v1/_ping` shipped this sprint. 38 unit tests pass; typecheck clean. **Phase 1 Sprint 1.2 complete 2026-04-25** — `verify()` + `verifyBatch()` ship with the non-negotiable compliance contract: 4xx ALWAYS throws (caller bug / scope / 422 / 404 / 413 — failOpen flag NEVER masks a real validation/scope error), timeout/network/5xx + `failOpen=false` (default) throws `ConsentVerifyError` wrapping the cause, timeout/network/5xx + `failOpen=true` returns `OpenFailureEnvelope` with `cause: 'timeout' | 'network' | 'server_error'` discriminator + traceId. New `src/types.ts` mirrors the §5.1 wire-format envelopes verbatim (snake_case responses, camelCase SDK inputs) + `IdentifierType` literal union + `OpenFailureEnvelope`. `verifyBatch` enforces client-side gates BEFORE network: empty array → RangeError, > 10 000 → RangeError matching server cap (saves the 413 round-trip), exactly 10 000 allowed at boundary, non-string entry → TypeError. New `isOpenFailure()` ergonomic type guard. Unit tests: 38 → **64 passing** (5 files, +26 over Sprint 1.1). **Phase 1 Sprint 1.3 complete 2026-04-25** — eight method modules + audit-trail wiring shipped. `recordConsent` (POST `/v1/consent/record`, idempotency-keyed via `clientRequestId`), `revokeArtefact` (POST `/v1/consent/artefacts/{id}/revoke`, URL-encoded path, 409 surfaces on terminal-state), `listArtefacts` + `iterateArtefacts` async-iterator + `getArtefact` (returns null on JSON-null body for unknown id), `listEvents` + `iterateEvents`, `triggerDeletion` (POST `/v1/deletion/trigger`, `purposeCodes` REQUIRED when `reason='consent_revoked'` enforced synchronously) + `listDeletionReceipts` + `iterateDeletionReceipts`, `createRightsRequest` (POST `/v1/rights/requests`, `identityVerifiedBy` required) + `listRightsRequests` + `iterateRightsRequests`, `listAuditLog` + `iterateAuditLog`. **Compliance-load-bearing**: new `onFailOpen` constructor option of type `FailOpenCallback = (envelope, ctx: { method }) => void | Promise<void>` closes Sprint 1.2's deferred audit-trail wiring. Default implementation: structured `console.warn`. Production callers wire to Sentry / structured logger / custom `/v1/audit` POST. Fire-and-forget — promise return NOT awaited; throws caught + emitted via `console.error` and never break the verify call site. `verify()` and `verifyBatch()` invoke it once after building the OpenFailureEnvelope. Unit tests: 64 → **94 passing** (7 files, +30 over Sprint 1.2). **Phase 1 Sprint 1.4 complete 2026-04-25 — Phase 1 closes.** Dual ESM+CJS build via `tsup` 8.5.1 (`tsup.config.ts` emits `dist/index.{mjs,cjs}` + `dist/index.d.{ts,cts}` + sourcemaps; tree-shake on; target `node18`). Build artefacts: 31.31 KB ESM / 31.53 KB CJS / 27.68 KB types. `package.json` flipped from TS-source to `dist/`: version 1.0.0-alpha.1 → **1.0.0**, conditional `exports` map, `sideEffects: false`, `prepublishOnly` script, `files: [dist, README.md, LICENSE.md]`. `LICENSE.md` proprietary text per CLAUDE.md authorship rules. README rewritten for v1.0.0: 14-method matrix + explicit fail-open behaviour table + onFailOpen default in compliance-posture table + pointer to both example directories. **Coverage gate ≥80%** wired via `@vitest/coverage-v8`; final coverage **94.82% statements / 90.56% branches / 95.31% functions / 95.52% lines**. New `tests/iterators.test.ts` (~9 cases) covers the four remaining async-iterator helpers + extra `recordConsent` / `triggerDeletion` validator branches. **Two integration examples**: `examples/express-verify-middleware/` (Express 5.1 middleware refusing with HTTP 451 on non-granted; HTTP 503 on fail-CLOSED; honours failOpen with `X-CS-Override` header) and `examples/nextjs-mobile-record/` (Next.js 16 App Router API route with module-load init, trace-id round-trip, structured error surface). Unit tests: 94 → **103 passing** (8 files, +9). **Spec amendment** — `npm publish` + staging smoke test deferred to operator follow-up (parallels Sprint 5.3's `vercel link` deferral; SDK is publish-ready: dist/ builds clean, prepublishOnly wired, version 1.0.0, LICENSE in place, README finalised). **Phase 1 (Node SDK) closes at 4/4 sprints.** **Phase 2 Sprint 2.1 complete 2026-04-25** — `consentshield` Python SDK at `packages/python-client/`. **Scope amendment**: ships **two clients** (sync `ConsentShieldClient` + async `AsyncConsentShieldClient`) with 100% method parity per the standard Python SDK convention (httpx, openai, anthropic, redis-py). 14 methods + 5 cursor-iterating helpers per client = 38 callable surfaces total. Shared core (`_config.py` validation, `_builders.py` request construction, `_verify.py` failure-decision) keeps duplication minimal between sync + async. Five-class error hierarchy mirrors the Node SDK 1:1. **Same compliance contract**: 4xx ALWAYS raises, fail-CLOSED default, fail-open opt-in returns OpenFailureEnvelope with cause discriminator, on_fail_open audit-trail callback (fire-and-forget on async; logging.warning default on both). 119 pytest tests passing; mypy --strict clean across 9 source files; coverage **88.54%** (over the 80% gate enforced via `pyproject.toml` `[tool.coverage.report] fail_under=80`). LICENSE.md + README.md (full method matrix + fail-open behaviour table) finalised. **Spec amendment** — `python -m build` / `twine upload` to PyPI + Django/Flask/FastAPI examples deferred to Sprint 2.2 (mirrors Sprint 1.4's deferral pattern). **Phase 2 Sprint 2.2 complete 2026-04-25 — Phase 2 closes.** Three integration examples shipped: `examples/django-middleware/` (`ConsentMiddleware` reading `settings.CONSENTSHIELD` for per-route purpose declarations, identifier resolved from JSON body / form / query / `X-CS-<field>` header in priority order, gates 451 / 503 / 502 + `X-CS-Override` on fail-OPEN; runnable demo via `urls.py` + `settings_demo.py`), `examples/flask-decorator/` (`@consent_required` decorator wrapping any view, identifier extraction caller-supplied, mirrors the Express middleware outcome contract), `examples/fastapi-dependency/` (async `Depends(consent_dependency(client, ...))` using `AsyncConsentShieldClient` natively — no thread-pool bridge — dependency resolves to the verify envelope so path operations can correlate via `evaluated_at` / `trace_id`; demo wires `lifespan` for clean `await client.aclose()` on shutdown). Build artefacts: `python -m build` produces `consentshield-1.0.0-py3-none-any.whl` + `.tar.gz`; verified by scratch-venv install on Python 3.9 (PEP 561 `py.typed` shipped, full 19-method surface intact on both clients). New `packages/python-client/PUBLISHING.md` is the first PyPI operator runbook in the repo — captures account/2FA setup, project-scoped API token, `twine check` + test-PyPI dry-run + production upload + recovery from a bad release. **Spec amendment** — final `twine upload dist/*` requires PyPI 2FA hardware key + token, identical to Sprint 1.4's npm publish deferral pattern. **Phase 2 (Python SDK) closes at 2/2 sprints.** **Phase 4 Sprint 4.2 complete 2026-04-25 — Phase 4 closes; Sprint 4.1 (Java) abandoned per scope amendment.** Go SDK at `packages/go-client/` ships full method parity with Node + Python: 14 named methods + 5 cursor paginators (`Iterate*`-returning `*Paginator` with `Next(ctx) bool` + `Page() []T` + `Err()`), `context.Context`-first signature, struct-options `*Params` per method, `errors.As`-discriminable five-class hierarchy (`*APIError`, `*NetworkError`, `*TimeoutError`, `*VerifyError`, `Error` interface for trace-id), zero runtime deps (pure stdlib). Same compliance contract: 4xx ALWAYS errors, fail-CLOSED default, `CONSENT_VERIFY_FAIL_OPEN=true|1` env override (with `WithFailOpen(...)` always winning over env). Two integration examples: `examples/nethttp-middleware/` (framework-agnostic `func(http.Handler) http.Handler` — drops into chi / gorilla/mux / alice) + `examples/gin-middleware/` (`ConsentRequired(client, opts) gin.HandlerFunc`). Both refuse with HTTP 451 on non-granted, 503 on fail-CLOSED, 502 on 4xx; `X-CS-Override` / `X-CS-Trace-Id` / `X-CS-Evaluated-At` correlation headers. **Coverage 87.8%** of statements via `httptest`-based tests (no network). New `packages/go-client/PUBLISHING.md` operator runbook codifies Go module proxy release flow (`git tag -a vX.Y.Z` → push → verify on `proxy.golang.org` → smoke install). **Spec amendment** — `git tag` + GitHub org `consentshield` reservation deferred to operator step (parallels npm publish from Sprint 1.4 + PyPI publish from Sprint 2.2). **Marketing-site SDK documentation also shipped this sprint:** `/docs/sdks` was a 404; now ships an index page (Node/Python/Go install matrix + compliance contract + cross-SDK quickstart) + three per-SDK landing pages at `/docs/sdks/node`, `/docs/sdks/python`, `/docs/sdks/go`. Wired into the docs sidebar (`marketing/src/app/docs/_data/nav.ts`) + search palette (`search-index.ts`). Old marketing copy "Node / Python / Java / Go" corrected to "Node / Python / Go" everywhere. **ADR-1006 sits at 7/9 (Phase 1 + 2 + 4 closed; Phase 3 OpenAPI CI drift check has 2 sprints remaining).** **Phase 3 Sprints 3.1 + 3.2 complete 2026-04-26 — Phase 3 closes; ADR-1006 closes at 9/9.** OpenAPI source of truth at `app/public/openapi.yaml` covers every public `/v1/*` endpoint (21 ops grouped by `Utility / Consent / Deletion / Rights / Audit / Account / Score / Security` tags). `info.license` declares Apache-2.0 (matches the SDKs); `servers` array leads with `https://api.consentshield.in/v1`. New `scripts/regenerate-whitepaper-appendix.ts` reads the spec, emits a Markdown table grouped by tag, and splices it into Appendix A of the v2 whitepaper between marker comments — first run replaces the hand-written table; subsequent runs are idempotent. Hand-rolled YAML parser (no new dep). `--check` mode exits non-zero on drift; wired into `app/package.json` `prebuild` so any deploy fails on drift, alongside `redocly lint` (zero warnings after the licence addition). `proxy.ts` updated to allow `/openapi.yaml` through the API-host gate so SDK code-generators can fetch from `api.consentshield.in/openapi.yaml`. Sprint 3.2's CI workflow step subsumed by the prebuild hook. **ADR-1006 closes at 9/9 sprints.** | Completed | 2026-04-19 | 9 | 9 |
| ADR-1007 | v2 Whitepaper Phase 7 — Connector ecosystem expansion (CleverTap, Razorpay, WebEngage/MoEngage, Intercom/Freshdesk, Shopify/WooCommerce, Segment) + WordPress + Shopify plugins | Proposed | 2026-04-19 | 3 | 9 |
| ADR-1008 | v2 Whitepaper Phase 8 — Scale + audit polish + P3 hardening (load tests, verify SLO, audit CSV, tracker corpus, multi-channel re-consent, HMAC rotation, SOC 2, React Native, WYSIWYG decision) | Proposed | 2026-04-19 | 3 | 10 |
| ADR-1009 | v1 API role hardening — DB tenant fence + `cs_api` direct-Postgres pool + service-role revoke (scope-amended 2026-04-21: direct Postgres, not HS256 JWT, due to Supabase key rotation) | Completed | 2026-04-20 | 3 | 8 |
| ADR-1010 | Cloudflare Worker — scoped-role migration off HS256 JWT to Hyperdrive (Supabase HS256 kill-timer). All four phases shipped 2026-04-22..24. Phase 1: prototype scaffold + Hyperdrive mechanism decision. Phase 2: cs_worker password rotated + BYPASSRLS + role-guard. Phase 3 Sprint 3.1: read paths (banner/origin/signatures) on postgres.js via env.HYPERDRIVE (Rule-16 carve-out: postgres@3.4.9 exact-pinned). Phase 3 Sprint 3.2: write paths (events/observations/worker-errors) — discovered + fixed `${JSON.stringify}::jsonb` double-encoding bug, switched to `sql.json()`. Phase 4 Sprint 4.1: deleted role guard + prototypes + probe route + SUPABASE_WORKER_KEY wrangler secret. Phase 4 Sprint 4.2 (2026-04-24): one request-scoped postgres.js client + `ctx.waitUntil(sql.end())` scheduled after the response; cold 2.9s → ~800ms, warm p50 60-100ms → 55-60ms; 10/10 live probes HTTP=200; readiness flag resolved at close. Sprint 4.3 (strip REST fallback) deferred to `docs/V2-BACKLOG.md` — Miniflare still needs the REST path for the 20 existing Worker-tests; not blocking. 9 integration tests PASS (5 reads + 4 writes); 20/20 Miniflare unchanged. | Completed | 2026-04-24 | 4 | 7 |
| ADR-1011 | ADR-1001 V2 C-1 — rotate+revoke 401→410 fix (revoked_api_key_hashes tombstone); landed inline 2026-04-21 | Completed | 2026-04-21 | 1 | 1 |
| ADR-1012 | v1 API day-1 DX gap fixes — `/v1/keys/self`, `/v1/usage`, `/v1/purposes`, `/v1/properties`, `/v1/plans` + OpenAPI examples backfill (Sprint 2.1 scope-amended to also fix pre-existing schema mis-placement + OpenAPI 3.1 nullable syntax) | Completed | 2026-04-21 | 2 | 4 |
| ADR-1013 | `cs_orchestrator` direct-Postgres migration (Next.js runtime) — completes ADR-1009 Phase 2 scope for the last scoped role on the customer-app surface. All five callers migrated (signup-intake, invitation-dispatch, dispatch helper, lookup-invitation, internal/invites, run-probes); architecture doc + Rule 5 synced; Next.js runtime fully off HS256. | Completed | 2026-04-21 | 2 | 3 |
| ADR-1014 | End-to-end test harness + vertical demo sites (partner-evidence grade) — Playwright + Supabase test project + Railway-hosted ecommerce/healthcare/BFSI demo sites; full-pipeline observable-state assertions; paired pos/neg + broken-on-purpose controls + Stryker mutation testing (≥ 80%); R2 evidence archive indexed at `testing.consentshield.in`; partner reproducible on own Supabase project in 30 min. **Phase 1 complete 2026-04-22** (harness foundations — 5/5 sprints). **Phase 2 complete 2026-04-23** (3 vertical demos + banner-embed matrix — 4/4 sprints; Playwright runtime-green deferred pending ADR-1010 Worker migration). **Phase 3 complete 2026-04-23** (full-pipeline E2E suites — 6/7 sprints; Sprint 3.2 R2-delivery + trace-id `[~]` partial, blocked on `consent_events.trace_id` column + Worker header propagation). **Phase 5 Sprint 5.1 complete 2026-04-25** — `scripts/partner-bootstrap.ts` (interactive CLI, hidden-input service-role key, idempotent, 30-min wall-clock budget, produces `.env.partner`). Unblocks ADR-1015 Phase 3. **Phase 5 Sprint 5.2 complete 2026-04-25** — `/docs/test-verification` reproduction runbook (MDX, under Reference in docs sidebar + Cmd-K palette). **Phase 5 Sprint 5.4 complete 2026-04-25** — 8 sacrificial controls in `tests/e2e/controls/` each targeting a distinct assertion matcher (string/integer/substring/cardinality/null-identity/regex/boolean/deep-equal) via Playwright `test.fail()` inversion. CI gate at `scripts/e2e-verify-controls.ts` (`bun run test:e2e:controls`) — walks results.json, reports SEV-1 on rogue controls. `/docs/test-verification/controls` runbook sibling to Sprint 5.2 page. **Phase 5 Sprint 5.3 complete 2026-04-25** — new `testing/` Bun workspace (Next.js 16 static site). List + run detail + per-vertical/sprint/phase filters + RSS 2.0 at `/feed.xml` + `/about` + `/robots.txt`. `PublishedRun` schema + git-tracked seed at `testing/src/data/runs.ts` (static data source; PR diff is the publication record). Code-complete + build-clean (9 prerendered pages); Vercel provisioning for `testing.consentshield.in` is operator follow-up per hard-to-reverse-ops policy. 24 static `/docs/*` routes prerender on marketing; `/docs/test-verification` updated with live link. **Phase 5 complete (4/4 sprints).** **Phase 4 Sprint 4.1 complete 2026-04-25** — Stryker 9.6.1 mutation baseline for the Worker security-critical surfaces (`hmac.ts` + `validateOrigin`/`rejectOrigin`). Score 91.07% (hmac 91.43 / origin 90.48); the dangerous `timingSafeEqual` length-equality bypass mutant is killed (oversized-signature attack); 5 surviving mutants documented as equivalent. New `worker/vitest.config.ts` + 49 unit tests + `worker/stryker.conf.mjs` + `bun run test:mutation` script with break-threshold gate at 80%. **Phase 4 Sprint 4.2 complete 2026-04-25** — Stryker baseline for the `deliver-consent-events` delivery pipeline. Per ADR-1019's Sprint 1.1 amendment the orchestrator is a Next.js route at `app/src/app/api/internal/deliver-consent-events/`, not a Deno Edge Function — so the Stryker scope lives in the `app` workspace under `app/stryker.delivery.conf.mjs`. Mutate scope: `lib/delivery/{canonical-json,object-key}.ts` + `lib/storage/endpoint.ts`. Score **95.65%** (canonical-json 100%, object-key 90.91%, endpoint 92.00%); 3 surviving mutants documented as equivalent. **Sprint 4.2 scope deviation** — `lib/storage/sigv4.ts` mutation kill-set deferred to a focused follow-up sprint (existing `sigv4.test.ts` pins URL shape only; killing internal signing-chain mutations needs pinned AWS sigv4 test vectors with mocked clock — ~1 sprint of focused work). **Phase 4 Sprint 4.3 complete 2026-04-25** — Stryker baseline for the v1 API pure helpers. **Spec amendment:** the original sprint targeted "the SECURITY DEFINER RPC wrappers" (`assert_api_key_binding`, idempotency-key, per-row fencing). Those are PL/pgSQL inside Postgres — Stryker can't mutate them. Sprint 4.3's actual mutate scope is the TypeScript surface fronting v1: `app/src/lib/api/{auth,v1-helpers,rate-limits}.ts` (Bearer-token verifier pre-SQL branches + RFC 7807 builder + scope/org gates + rate-tier dictionary). The PL/pgSQL RPC contracts themselves stay covered by the integration / RLS / Phase 3 E2E suites. Score **100.00%** (auth 100%, v1-helpers 100%, rate-limits 100%); zero survivors — the cleanest Phase 4 module set so far. New `app/tests/api/{auth,v1-helpers,rate-limits}.test.ts` (~55 cases authored from scratch; no pre-existing coverage on these surfaces). Three regex anchor / quantifier mutants on the Bearer pattern killed by a `'malformed'` (regex-fail) vs `'invalid'` (regex-pass + SQL-fail-via-catch) reason-code distinction. **Phase 4 Sprint 4.4 complete 2026-04-25** — Phase 4 closes. `scripts/run-mutation-suite.ts` aggregate driver runs the three Phase-4 Stryker configurations sequentially, computes per-module scores, writes `reports/mutation/summary.json`, exits 1 on any module < 80% break threshold (negative-control verified). `.github/workflows/mutation.yml` nightly schedule (04:30 UTC) + `workflow_dispatch` + 30-day HTML-report artefact upload. **Spec amendment** — original "publishes HTML to testing.consentshield.in/runs/<sha>/mutation/" predated Sprint 5.3's "no upload pipeline" decision; Sprint 4.4 publishes per-module **scores + counts** as structured fields on the `PublishedRun` schema (extended with `mutation: ModuleMutationScore[] | null`) while per-mutant HTML stays as CI artefacts. New published run at `testing.consentshield.in` (commit 55d6275; aggregate score 95.57; sprint tags 4.1/4.2/4.3/4.4) with the per-module breakdown rendered as a colour-coded table on the run-detail page. Partner-readable explainer at `/docs/test-verification/mutation-testing` (~165 lines; what's-in-scope + what's-out-of-scope + how-to-read-scores + run-locally + CI gate + 5-row FAQ; nav + Cmd-K wired). **Phase 4 sigv4 follow-up complete 2026-04-25** — closes the Sprint 4.2 deferral. New `app/stryker.sigv4.conf.mjs` + `app/tsconfig.stryker.sigv4.json` + `scripts/capture-sigv4-vectors.ts` + extended `app/tests/storage/sigv4.test.ts` (12 → 28 cases). Pinned AWS sigv4 vectors with frozen clock (`Date.UTC(2026, 0, 15, 8, 0, 0)`) for `presignGet`, `putObject`, `deleteObject`, `probeHead/Get/Delete/List`. Score **78.26%** (covered 83.24%) — iteration trace 25% baseline → 65.76% (pinned PUT/DELETE/PRESIGN) → 76.09% (PROBE pinned + sha256Hex Buffer/Uint8Array) → 77.72% (URL exact-match + metadata-headers assertion + 5xx body excerpt) → 78.26% (probeGet + probeDelete pinned method literals). 29 surviving mutants documented as equivalent (redundant sort comparators given pre-sorted inputs / Hash.update polymorphism / `keyForPath==''` ternary producing same canonical-uri / arrayBuffer drain optimization / drop-`.catch` on cleanly-resolving body). **Carve-out break threshold of 75** for sigv4 in `scripts/run-mutation-suite.ts` reflects the equivalent floor without violating Rule 13 (no `// Stryker disable` comments). New published run `06EW0PT8M5XKDV6N9R3FB72JKQ` on testing.consentshield.in; aggregate four-module score 91.24%. **Sprint 3.2 closed 2026-04-25** — trace-id wire shipped: migration `20260804000058_adr1014_s32_consent_events_trace_id.sql` adds nullable `trace_id text` to `public.consent_events` with partial index `WHERE trace_id IS NOT NULL`; `worker/src/events.ts` derives the trace id (`X-CS-Trace-Id` header → trim + 64-char clamp; missing → 16-char hex via `crypto.randomUUID`), persists it on the row, and echoes it back via the `X-CS-Trace-Id` response header (with `Access-Control-Expose-Headers` so cross-origin browsers can read it); every Response now carries the trace id (success + error paths) via the `withTraceId(...)` helper; `tracedRequest` Playwright fixture sets both `X-Request-Id` and `X-CS-Trace-Id`; `tests/e2e/worker-consent-event.spec.ts` asserts the round-trip three ways (echoed in response + persisted on row + matches inbound id); new `worker/tests/trace-id.test.ts` (8 cases) covers `deriveTraceId` propagate / trim / clamp / generate / blank / freshness branches. **ADR-1014 now sits at 24/24 sprints complete + Phase 4 + sigv4 follow-up closed; status flips In Progress → Completed.** Sibling of ADR-1015. | Completed | 2026-04-22 | 5 | 24 |
| ADR-1015 | v1 API integration tests + customer developer documentation — external-consumer Bearer-key test suite across all 15 `/v1/*` endpoints + public `/docs/*` surface on marketing (Next.js MDX + `@scalar/api-reference`): dev hub, quickstart, 6 concepts, 7 cookbook recipes, endpoint reference, interactive playground. Wireframe in `docs/design/screen designs and ux/consentshield-developer-docs.html`. **Phase 1 complete 2026-04-24** (shell + Scalar + search). **Phase 2 Sprint 2.1 complete 2026-04-24** — 10 MDX pages: Developer Hub, Quickstart, 6 concept pages, Authentication, Rate-limits. **Phase 2 Sprint 2.3 complete 2026-04-24** — reference tier: `/docs/errors` (RFC 7807 catalogue grounded in the 54 live `problemJson` variants), `/docs/changelog` (ADR-1001/1002/1011/1012 + deprecation policy), `/docs/webhook-signatures` (three HMAC-SHA256 schemes with Node/Python/Go samples), `/docs/status` (external status-page pointer). **Phase 2 Sprint 2.2 complete 2026-04-24** — cookbook tier: 7 recipes × cURL + Node + Python (Go added on the deletion-webhook recipe). **Phase 4 Sprint 4.1 + 4.3 complete 2026-04-24** — cross-link audit (fixed 3 broken deep-links, expanded ENDPOINTS catchall to Audit/Rights/Security/Score tags, verified 5 in-page `#slug` anchors against rendered HTML) + docs-issue GitHub Issue Forms template with FeedbackStrip pre-fill wiring. 22 static `/docs/*` routes + 1 dynamic catchall. **Phase 3 unblocked 2026-04-25** — ADR-1014 Sprint 5.1 (`scripts/partner-bootstrap.ts`) produces the `.env.partner` shape the integration harness needs; Sprint 3.1 can now pick up the fixture factory. Sprint 4.2 (wireframe reconciliation) remains deferred pending human visual QA. | In Progress | 2026-04-22 | 4 | 17 |
| ADR-1016 | v1 API — close the orphan `read:audit`, `read:security`, `read:score` scopes. Three thin SECURITY DEFINER RPCs over existing tables (audit_log, security_scans, depa_compliance_metrics); all fenced + granted to cs_api. 3 of 7 orphan scopes closed. `cs_api` EXECUTE surface 19 → 22 RPCs. | Completed | 2026-04-22 | 1 | 3 |
| ADR-1017 | Admin ops-readiness alerts — `admin.ops_readiness_flags` table + RPCs + seed + `/admin/(operator)/readiness` panel. Surfaces external blockers (legal counsel, partner engagement, PagerDuty provisioning, SE hire, SLA drafting, wrangler cutover) so ADR sprint handoffs don't lose track of pending real-world items. All three sprints shipped 2026-04-22; Sprint 1.3 also carried the audit-log column-misuse fix (migration `20260804000019`) surfaced while writing tests. 12 ops-readiness + 11 status-page assertions all green. Runbook at `docs/runbooks/ops-readiness-flags.md`. | Completed | 2026-04-22 | 1 | 3 |
| ADR-1018 | Status page — Phase 1 in-app self-hosted (Completed 2026-04-23, superseded 2026-04-25), Phase 2 vendor pivoted Better Stack → self-hosted Uptime Kuma on Railway (2026-04-26). Phase 2 In Progress: Kuma instance + DNS + KUMA_API_KEY landed; Sprints 2.2 (monitor matrix) → 2.8 (marketing copy alignment) operator-pending per `docs/runbooks/adr-1018-phase-2-uptime-kuma.md`. Phase 1 in-app readouts retained as in-perimeter triage view. | In Progress | 2026-04-22 | 2 | 13 |
| ADR-1019 | `deliver-consent-events` Edge Function — the missing R2 export path. Closes the gap referenced by ADR-0022 / ADR-0023 / ADR-0012 / ADR-1014: `delivery_buffer` + `export_configurations` schemas + `cs_delivery` grants all exist, but no delivery function has ever shipped. Reads buffered rows under `cs_delivery`, decrypts per-org R2 credentials, uploads canonical-serialised JSON to customer bucket, DELETEs in the same transaction as the confirmed upload. Trigger-fired primary path + pg_cron 60s safety net (mirrors `process-artefact-revocation`). Per-row exponential backoff + manual-review escalation after 10 failures. Integrates with ADR-1018 status page (`delivery-pipeline` subsystem) + ADR-1017 readiness flags (`delivery_retry_exhausted`). Unblocks ADR-1014 Sprint 3.2's R2 hash-match positive. | Proposed | 2026-04-23 | 4 | 7 |
| ADR-1020 | Multilingual support per MeitY BRD §4.1.1 (umbrella). BRD-mandatory surfaces only — Worker banner + public rights portal + Privacy Center + status page + DP email/SMS templates + grievance submission. Tenant dashboard + marketing stay English. Phase 1 ships 7 langs (Hindi + Tamil + Telugu + Bengali + Marathi + Gujarati + Kannada); Phase 4 completes the remaining 15 Eighth Schedule languages. Sarvam AI primary + Claude QA via Vercel AI Gateway; IndicTrans2 fallback for low-resource tail langs. Permanent `(source_hash, lang, glossary_version)` translation cache — AI called at most once per tuple across system lifetime. Language detection cascade with always-visible selector; MT fallback defaults to English + "other languages" chip; tenant review console gates approval of machine-translated banner/purpose text; ABDM-framework purposes require tenant upload (DB trigger refuses MT). Companion plan at `docs/plans/adr-1020-multilingual-support.md`. | Proposed | 2026-04-23 | 4 | — |
| ADR-1021 | i18n foundation (Phase 1 of ADR-1020). Four schema migrations (`ui_translation_strings`, `tenant_content_translations` + ABDM-lock trigger, `i18n_translation_cache` + glossary-version counter, `i18n_glossary` + column additions on 7 existing tables). `packages/i18n-tools/` Bun workspace with provider router (Sarvam / Claude / IndicTrans2), cache-first `translate()` wrapper, glossary-as-tool-schema for Claude, sentinel-substitution for Sarvam, AST extraction via ts-morph, jscodeshift codemod wrap. GitHub Action CI gate: extract-translate-QA-commit-back to PR branches via `consent-shield-bot`. No user-facing UI changes in Phase 1. | Proposed | 2026-04-23 | 1 | 3 |
| ADR-1025 | Customer storage auto-provisioning — hybrid managed R2 default + BYOK escape hatch. Upstream of ADR-1019: `deliver-consent-events` can't run without populated `export_configurations` rows; this ADR populates them. Tier 1 default: at onboarding Step 4, a background job derives a sha256-prefixed per-org bucket name, creates it via Cloudflare R2 REST API under CS's account, generates a bucket-scoped API token, encrypts both with the per-org HMAC-derived key, runs a PUT/GET/DELETE verification probe, flips `is_verified=true` — all without user interaction. Wizard advances non-blocking; Step 7 shows a soft banner if verification is still in flight. Tier 2 BYOK: `/dashboard/settings/storage` (account_owner-gated) accepts R2 / S3 credentials, runs the probe, offers copy-existing-and-cutover or cutover-forward-only migration modes; `migrate-customer-storage` Edge Function streams historical objects across resumably, atomically swaps the pointer, revokes the CS-managed token. Nightly verify cron catches silently-revoked BYOK tokens. Rule 4 re-framed: the canonical compliance record being in R2 is what matters; CF account ownership is a commercial/procurement layer on top. DPA + per-tenant isolation + scoped tokens satisfy the rule in both tiers. Originally drafted as ADR-1022; renumbered so ADR-1020's reserved 1022/1023/1024 slots for multilingual Phase 2/3/4 stay intact. | Proposed | 2026-04-23 | 4 | 7 |
| ADR-1026 | Rewind ADR-1010 Phase 3's *mechanism* choice — Worker connects directly to Supavisor, drop the Hyperdrive binding. **Abandoned 2026-04-24** after Sprint 1.2 discovered CF Workers free-tier 50-subrequest-per-invocation limit blocks direct-Supavisor (cold TLS + SCRAM handshake alone exceeds 50; max:1 doesn't help — budget is consumed in the handshake, not the pool size). Hyperdrive's internal `*.hyperdrive.local` hostname is the actual load-bearing value: its connections don't count toward the 50 (not "edge pool latency" or "query caching" as the ADR originally claimed). Product-owner decision: keep Hyperdrive, stay on CF free plan, accept intermittent `CONNECTION_CLOSED` as a monitored-but-tolerated failure mode (KV 5-min cache absorbs most cold paths). Reopen triggers documented in the ADR's Abandonment note: (a) Workers Paid adopted for another reason, (b) Hyperdrive failures escalate to customer-visible, (c) CF removes the free-tier subrequest limit. Sprint 1.1's `CS_WORKER_DSN` wrangler secret deleted at abandonment. Operational state: Worker on Sprint 4.2 Hyperdrive build (version 9b5b3024). | Abandoned | 2026-04-24 | 1 | 5 |
| ADR-1027 | Admin account-awareness pass — closed every operator-surface drift remaining after ADR-0044 / 0048 / 0050 / 0055 / 0056 landed the accounts layer. All seven sprints shipped 2026-04-24: (1.1) `admin_audit_log.account_id` column + trigger + backfill + filter; (1.2) `admin.admin_dashboard_tiles()` RPC + account-tier tiles + accounts-by-plan histogram + trial-to-paid gauge; (2.1) `<AccountContextCard>` component + org-detail compact strip + Pipeline group-by-account toggle with per-account rollup; (2.2) support ticket parent-account header + account filter; (3.1) `admin.impersonation_sessions_by_account()` RPC + new `/impersonation-log` panel with per-session / per-account tabs; (3.2) `admin.account_notes` + four CRUD RPCs + account-detail Notes card + org-detail read-only surfacing; (3.3) `accounts.default_sectoral_template_id` + admin picker card + customer wizard Step 4 pre-selection with teal-highlighted "Account default" badge. 24 integration tests across 5 test files; typecheck + lint + `supabase db push` clean on live dev. | Completed | 2026-04-24 | 3 | 7 |
| ADR-1028 | Generated server-side SDKs — Java + .NET + PHP via OpenAPI Generator. Closes the v2 whitepaper §5.4 multi-language promise (Tier 2 of the three-tier SDK split codified in this ADR: Tier 1 = hand-rolled Node/Python/Go, shipped in ADR-1006; Tier 2 = OpenAPI-generated Java/.NET/PHP, this ADR; Tier 3 = mobile Swift/Kotlin, deferred until ABDM mobile trigger). Spec at `app/public/openapi.yaml` is the input; thin hand-written wrappers add framework integration (Spring Boot auto-config / ASP.NET Core DI / PSR-18). v1 generator: OpenAPI Generator (free, open-source); Speakeasy / Stainless / Fern re-evaluation gated as a Phase 1 follow-up after v1.0.0 output is in hand. Same compliance contract as Tier 1: 4xx ALWAYS surfaces, fail-CLOSED default with `CONSENT_VERIFY_FAIL_OPEN` env override, `X-CS-Trace-Id` round-trip, 2 s per-attempt timeout + 100/400/1600 ms backoff (5xx + transport only). Apache-2.0 LICENSE + NOTICE trademark carve-out matches Tier 1. Maven Central + NuGet + Packagist publish flows; Sonatype OSSRH onboarding is the longest-lead step (Phase 1 starts there). 4 phases / 8 sprints estimated. **Phase 1 Sprint 1.1 shipped 2026-04-26** — generator pipeline + CI drift gate. **Sprint 2.1 shipped 2026-04-26** — `packages/dotnet-client/wrapper/ConsentShield.Client.AspNetCore` (NuGet `ConsentShield.Client.AspNetCore:1.0.0`) on .NET 8.0 LTS. `IServiceCollection.AddConsentShield(IConfiguration)` + inline `Action<ConsentShieldOptions>` overload. `IHttpClientFactory` named client + Bearer auth + `RetryHandler` `DelegatingHandler` (same 100/400/1600 ms contract; never retries 4xx; per-attempt `TaskCanceledException` translated to `ConsentShieldTimeoutException` while caller-cancellation propagates intact). Three-class exception hierarchy + `VerifyFailureCause` enum. ASP.NET Core 8 example at `examples/AspNetCoreMarketingGate/`. PUBLISHING.md (NuGet runbook). 27/27 tests pass via `mcr.microsoft.com/dotnet/sdk:8.0` Docker image. **Sprint 1.2 shipped 2026-04-26** — `packages/java-client/wrapper/` Spring Boot starter (`com.consentshield:consentshield-java-spring-boot-starter:1.0.0`) with `ConsentShieldClient` factory, three-class exception hierarchy (`ConsentShieldApiException` / `ConsentShieldTimeoutException` / `ConsentVerifyException` + `ConsentVerifyOutcome`), `RetryInterceptor` (100/400/1600 ms backoff, never retries 4xx, never retries timeouts), `@AutoConfiguration` + `ConsentShieldProperties` bound under `consentshield.*`. Spring deps `<optional>true</optional>` so non-Spring callers don't pull Spring transitively. Spring Boot 3 example at `examples/spring-boot-marketing-gate/`. PUBLISHING.md (Sonatype Central runbook). 22/22 wrapper tests, JaCoCo ≥ 80%. Generator pinned to `openapitools/openapi-generator-cli:v7.10.0`. Speakeasy bake-off **deferred to Q3/Q4 2026** unless marketing escalates (review at `docs/reviews/2026-04-26-tier2-generator-evaluation.md`). | In Progress | 2026-04-26 | 4 | 8 |
| ADR-0501 | ConsentShield marketing site (`marketing/`) — Bun workspace sibling; scaffold → content → downloads (PDF/DOCX/MD) → security hardening | In Progress | 2026-04-21 | 4 | 4+ |
| ADR-0502 | Marketing-site invite gate via email OTP (`marketing/src/lib/gate/*` + `/gate` page + middleware). Two-layer confidential-preview gate — Layer 1 (robots / noindex / X-Robots-Tag, commit `e50a5ab`) + Layer 2 (this ADR — HS256 session + Resend OTP + per-IP rate limit + structured logging of five `gate.*` events). Replaces Vercel Password Protection ($150/mo). Sprints 1.1–1.3 shipped 2026-04-25; Sprint 1.4 (env-var seeding + first-pass smoke) operator-pending. | In Progress | 2026-04-25 | 1 | 4 |

<!--
When adding a new ADR:
1. Assign the next sequential number
2. Add a row to this table
3. Keep status updated as work progresses

ADR number ranges (kept non-overlapping so parallel tracks don't collide):
- 0001..0019 — Phase 1 customer app + DEPA roadmap charter
- 0020..0025 — DEPA execution
- 0026..0049 — admin platform (monorepo, schema, per-panel admin ADRs)
- 0050..0057 — admin-billing + admin-extensibility (issuers, invoices, GST,
  disputes, account-scoped impersonation, account-scoped feature flags)
- 0501+       — marketing site (consentshield.in)
- 1001+       — v2 whitepaper (public API + connector ecosystem)
-->
